/*

//Leg 1 = FR
float theta1, theta2, theta3 = 0;

//Leg 2 = BR
float theta4, theta5, theta6 = 0;

//Leg 3 = FL
float theta7, theta8, theta9 = 0;

//Leg 4 = BL
float theta10, theta11, theta12 = 0;

*/
//Coordinates
int x_coord, y_coord, z_coord;

//Leg dimensions
int coxa, tibia, femur;

//Joint Angles
float f_angle,t_angle, c_angle;

float legLength;

float hf,a1,a2,b1;




void inverseKinematics(){
  /*
  legLength = sqrt(sq(x_coord) + sq(y_coord));

  hf = sqrt(sq(legLength-coxa)+ sq(y_coord));
  
  a1 = atan2(legLength-coxa,y_coord);
  
  a2=acos(tibia);
  
  println("hello");
  */
}